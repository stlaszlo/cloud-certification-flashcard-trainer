/**
 * Exam pack shape:
 * {
 *   id, provider, certification, title, code, color, durationMinutes,
 *   officialQuestionRange, description, domains[], caseStudies[], questions[]
 * }
 *
 * Question shape:
 * {
 *   id, domain, caseStudy?, difficulty?, type: "single" | "multiple", prompt,
 *   options: [{ id, text }], correct: ["option-id"], explanation, objective
 * }
 */
export {};
